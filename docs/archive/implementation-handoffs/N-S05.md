# N-S05 handoff

Status: **complete**

## Session identity

- Base SHA: `cb043e96a67acb2110d5382ec50f46b788a96e00`
- Branch: `codex/neon-s05-portable-business-baseline`
- Commit: self-referential; this handoff is included in the single logical
  S05 commit, whose exact SHA is reported in the session completion response
- Scope: provider-neutral PostgreSQL business tables, identity columns,
  constraints, indexes, final views, machine-readable inventory and
  empty-PostgreSQL clean-room assertions

## Preflight and integration

- Read completely: `AGENTS.md`,
  `specs/2026-07-30-neon-migration-and-multitenancy.md` and
  `docs/implementation-handoffs/N-S04.md`.
- Confirmed the working tree was clean before S05 changes.
- Confirmed `codex/neon-s04-schema-inventory` pointed to
  `cb043e96a67acb2110d5382ec50f46b788a96e00`.
- Confirmed `main` was at S03 `2e64436231ae0dba7cfc1c9b6570349863e591cf`.
- Integrated S04 into `main` with `git merge --ff-only`, then created this
  branch at `cb043e96...`; no reset or rebase was used.

## Implementation

- Added `postgres/tenant-baseline/v1/001_portable_business_baseline.sql` as a
  clean-room, provider-neutral baseline containing all 25 business tables,
  13 identity columns, 63 check constraints, 25 primary keys, 9 unique
  constraints, 28 foreign keys, 37 explicit indexes and 7 final views.
- Preserved source table definitions, defaults, nullability, identity
  sequences, business keys, FK delete actions, check semantics, milestone
  columns and view output definitions. The source's eight view declarations
  are represented by the seven current view objects; `campaign_metrics` is
  declared once using its final definition.
- Omitted the source `team_members` identity-provider reference column and
  its provider FK/index. S06 owns canonical users and provider mappings.
- Added a machine-readable inventory with table column definitions, index and
  constraint inventories, identity sequences, view output columns and
  explicit deferred v053 dependencies.
- Added static source-contract assertions and a Docker clean-room harness with
  live PostgreSQL catalog assertions.
- Preserved the server-owned API and non-owner/non-`BYPASSRLS` intent in the
  documentation; absence of RLS in this baseline is not an owner/bypass path.

## Changed files

- `postgres/tenant-baseline/v1/001_portable_business_baseline.sql`
- `postgres/tenant-baseline/v1/README.md`
- `docs/platform-ops/portable-business-schema-inventory-v1.json`
- `postgres/tests/portable_business_inventory_assertions.mjs`
- `postgres/tests/portable_business_catalog_assertions.sql`
- `postgres/tests/portable_business_cleanroom.sh`
- `docs/implementation-handoffs/N-S05.md`

No historical Supabase migration, v053 baseline, runtime SQL, API, UI,
DataContext, Auth, Storage, sync-agent, ingest, operations or TypeScript file
was changed.

## Verification

- `node --check postgres/tests/portable_business_inventory_assertions.mjs` —
  passed.
- `node postgres/tests/portable_business_inventory_assertions.mjs` — passed:
  25 tables, 7 views, 13 identity columns, 71 total indexes, 125 total
  constraints and 28 foreign keys; source definitions and forbidden-provider
  marker checks passed.
- `POSTGRES_IMAGE=postgres:16-alpine postgres/tests/portable_business_cleanroom.sh`
  — passed. The baseline applied to an empty local PostgreSQL container and
  live catalog assertions passed; the ephemeral container was removed by the
  harness.
- `bash -n postgres/tests/portable_business_cleanroom.sh` — passed.
- `cd frontend && npm test` — passed, 9 test files / 49 tests.
- `cd frontend && npm run build` — passed (`tsc -b` and Vite production
  build); no TypeScript files were changed by S05.
- `git diff --check` — passed.

## External state and secrets

No external mutation occurred: no Neon project, database, R2 bucket, Auth
user, Vercel configuration, deployment, credential, Supabase change, dump,
data copy or Git push was created. Docker Desktop was used only for a local
ephemeral clean-room container, which was removed after the assertions. The
portable baseline and inventory contain no secrets, credentials or
provider-specific resource IDs.

## Known limits

- S05 intentionally defers canonical users/provider mappings, runtime roles,
  actor context, `auth.uid()`-style request context, RLS and policies.
- S05 intentionally defers portable functions/triggers, the SELECT-only AI SQL
  guard, Storage metadata/object-provider integration and migration ledger.
- The clean-room test proves application on local PostgreSQL 16; it does not
  create or inspect a Neon project. S08 owns the later two-apply, provider
  ledger and dump/restore/reconciliation harness.
- The baseline is schema-only and contains no business data.

## Exact start point for S06

S06 starts only after this S05 commit and its fast-forward integration into
`main`. S06 owns canonical application users, provider mappings, runtime roles,
request-scoped actor context and RLS SQL. S06 must keep business tables and
views provider-neutral and must not reintroduce provider identity/runtime
dependencies into `postgres/tenant-baseline/v1/001_portable_business_baseline.sql`.

**S06 не начиналась.**
