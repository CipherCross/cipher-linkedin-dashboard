# N-S03 handoff

Status: **complete**

## Session identity

- Base SHA: `77c0bf336daae7bfe675bd7861d7b83b8738f6c6`
- Branch: `codex/neon-s03-datastore-contracts`
- Commit: self-referential; this handoff is included in the single logical
  S03 commit, whose exact SHA is reported in the session completion response
- Scope: provider-neutral `DataStore` contracts, fake implementation and
  contract tests only

## Preflight and integration

- Read completely: `AGENTS.md`,
  `specs/2026-07-30-neon-migration-and-multitenancy.md` and
  `docs/implementation-handoffs/N-S02.md`.
- Confirmed the supplied S02 branch pointed to
  `77c0bf336daae7bfe675bd7861d7b83b8738f6c6` and the working tree was clean.
- Confirmed `main` was at S01 `794875dbd5ff5a634115037f4cc4770f3236ecd2`.
- Integrated S02 into `main` with `git merge --ff-only`; no reset or rebase was
  used.
- Created this branch from the resulting S02 SHA.

## Implementation

- Added named, allowlisted query and command descriptors without SQL text,
  driver types, provider resource IDs or credentials.
- Added canonical user, machine and system actor contexts. Anonymous callers
  cannot satisfy the contract; operation handlers receive the validated actor
  and own their authorization checks.
- Added UTC timestamp normalization and half-open `[fromInclusive,
  toExclusive)` ranges. Offset-bearing inputs normalize to a `Z` timestamp.
- Added bounded page requests and opaque continuation pages. The fake binds a
  cursor to operation, query shape, tenant and actor scope.
- Added transaction callbacks with actor context and atomic fake snapshots;
  callback and command failures leave committed state unchanged.
- Preserved server-owned, non-owner and non-bypass-row-security intent as an
  explicit `DataStore` security contract.

## Changed files

- `frontend/api/_lib/data/contracts.ts`
- `frontend/api/_lib/data/fake.ts`
- `frontend/api/_lib/data/index.ts`
- `frontend/tests/dataStore.test.ts`
- `docs/implementation-handoffs/N-S03.md`

No existing application handler or frontend consumer was wired to this layer.

## Verification

- `cd frontend && npm test -- tests/dataStore.test.ts` — passed, 9 tests.
- `cd frontend && npm test` — passed, 9 test files / 49 tests.
- Dedicated TypeScript check for the new contracts, fake and test — passed:
  `tsc --noEmit --target ES2022 --module ESNext --moduleResolution Bundler
  --strict --skipLibCheck --allowImportingTsExtensions --noUnusedLocals
  --noUnusedParameters` with the four S03 TypeScript entrypoints.
- `cd frontend && npm run build` — passed (`tsc -b` and Vite production build).
- `git diff --check` — passed.
- Scope and secret review — only the files listed above changed; no secret,
  credential, connection string or provider resource identifier was added.

## External state and secrets

No external mutation occurred: no Neon project, database, R2 bucket, Auth user,
Vercel configuration, deployment, credential, Supabase change, dump or data
copy was created. The only Git ref changes were the required local S02
fast-forward into `main` and creation of this local S03 branch. Nothing was
pushed.

## Known limits

- `FakeDataStore` is an in-memory contract test double; it does not implement a
  SQL engine, driver, connection lifecycle, isolation level or provider call.
- Real operation-to-query mapping, runtime-role setup and database rollback
  behavior remain adapter work for S11.
- The fake cursor registry is process-local and intentionally does not model a
  durable/provider cursor encoding.
- No current Supabase handler, browser API or DataContext was migrated in S03.

## Exact start point for S04

Start S04 from the single S03 commit containing this handoff, after it is
merged into `main` (the exact self-referential SHA is in the completion
response). S04 owns only the machine-readable inventory of the current v053
baseline and its Supabase-only dependency list, using S02's accepted G0
decisions and this contract boundary. S04 must not implement the portable
business baseline, Neon adapter, UI, identity, storage, ingest or operations
runtime.

**S04 не начиналась.**
