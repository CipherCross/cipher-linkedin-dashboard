# N-S06 repair handoff

Status: **complete**

## Session identity

- Final repair base: `de0dde3778793098037890ef74faf2ca39345aaa`
- Original S06 repair point: `6be548559e22a54cc3f1cdd2a5a7468da8968bcb`
- Branch: `codex/neon-s04-s06-final-fixes`
- Commit: self-referential; the exact final repair SHA is reported in the
  session completion response
- Scope: final S04 assertion/inventory review fixes and S06 clean-room role
  bootstrap correction

## Final review fixes

- S04 now documents `functions[].execute_to` as explicit final grant roles,
  not a complete effective PostgreSQL ACL. The assertion replays the source
  GRANT/REVOKE order, compares normalized function names plus ordered argument
  types, checks owner/language/security-definer/search_path, and separately
  verifies PUBLIC/anon/authenticated denials for functions with an explicit
  ACL boundary, except for roles listed in `execute_to`.
- S04 FK assertions now compare constraint name, source table, local column
  order, target schema/table and target column order, ON DELETE, and the
  explicit/default ON UPDATE action. RLS assertions compare exact policy
  name/table/command/role/normalized USING definitions; the authenticated
  policy is exactly `public.is_active_team_member()` and the AI policy is
  exactly `true`.
- The S04 inventory SHA changed to
  `0b9b0b987bb580bbcf8a5960b9d367fb0fe5b550d3b968ab9957df4a290563f6` and
  the S05 portable inventory pin and related semantic assertion were updated.
  The immutable v053 baseline SQL was not changed.
- S06 test-only role bootstrap now transfers ownership in deterministic
  tables → views → sequences order. It never uses broad `REASSIGN OWNED`.
  The standard clean-room harness is unchanged.

## S06 contract retained and verified

- `app_owner` is a non-login, non-superuser object owner.
- `app_migration` is a separate non-superuser login with only `SET ROLE
  app_owner`; it owns no public objects and applies `002_identity_roles_actor_rls.sql`.
- `app_runtime` is a separate non-superuser login with no owner/migration
  membership, no object ownership and no `BYPASSRLS`; both `SET ROLE
  app_owner` and `SET ROLE app_migration` fail from its session.
- `app_readonly` remains read-only. `app_machine` and `app_system` remain
  no-login, no-grant, fail-closed principals. No fake team users, owner bypass,
  provider-specific identity columns or provider roles were added.
- S03 ActorContext remains authoritative: S06 authorizes only canonical active
  user actors by `users.id` and transaction-local `app.actor_id`. Positive
  active-user DML and negative missing, malformed, unknown, inactive and
  cross-user cases are covered. Machine/system identity is deliberately not
  invented in S06: S21 owns machine identity/ingest and S15 owns system/job
  identity; these future owners are recorded as fail-closed boundaries.
- The S06 artifact SHA remains
  `18a779f3abd99592a1af71430f87b4f93c7beff3fba75924fa0122ae0b3c3d80`.
  The portable README, inventory, role catalog, runtime boundary and actor/RLS
  assertions continue to pin this contract.

## Changed files in this final repair

- `docs/platform-ops/tenant-schema-inventory-v053.json`
- `docs/platform-ops/portable-business-schema-inventory-v1.json`
- `postgres/tests/portable_business_inventory_assertions.mjs`
- `postgres/tests/portable_identity_roles_rls_role_bootstrap.sql`
- `supabase/tests/tenant_inventory_assertions.mjs`
- `docs/implementation-handoffs/N-S06.md`

The S06 artifact, portable identity inventory, README, catalog assertions,
behavior assertions, fixture seed and unchanged clean-room harness remain in
the repair base and are included in the final branch without unreviewed
changes.

## Verification

- `node --check supabase/tests/tenant_inventory_assertions.mjs` — passed.
- `node supabase/tests/tenant_inventory_assertions.mjs` — passed: 25 tables,
  8 view declarations / 7 final view objects, 13 functions, 12 triggers, 72
  indexes, 126 constraints, 25 RLS tables, 50 policies and 17 dependencies.
- `node --check postgres/tests/portable_business_inventory_assertions.mjs` and
  `node postgres/tests/portable_business_inventory_assertions.mjs` — passed;
  S04 SHA pin, ACL semantics, archive ACL and agent-artifact dependency checks
  passed.
- `node --check postgres/tests/portable_identity_roles_rls_inventory_assertions.mjs`
  and `node postgres/tests/portable_identity_roles_rls_inventory_assertions.mjs`
  — passed; S06 artifact SHA remained pinned.
- `bash -n postgres/tests/portable_business_cleanroom.sh` and
  `bash -n postgres/tests/portable_identity_roles_rls_cleanroom.sh` — passed.
- `POSTGRES_IMAGE=postgres:17-alpine postgres/tests/portable_business_cleanroom.sh`
  — passed: business catalog clean-room apply.
- `POSTGRES_IMAGE=postgres:17-alpine postgres/tests/portable_identity_roles_rls_cleanroom.sh`
  — passed: artifact applied via `app_migration`; runtime boundary, SET ROLE
  denials and active/missing/malformed/unknown/inactive/cross-user DML/RLS
  assertions passed.
- `jq empty` over all four relevant inventory JSON files — passed.
- `git diff --check` — passed.
- `cd frontend && npm test` — passed, 9 test files / 49 tests.
- `cd frontend && npm run build` — passed (`tsc -b` and Vite production
  build; only the existing chunk-size warning was emitted).
- `cd ops && npm test` — passed, 39 tests.

## Ownership boundaries after S06

- **S07** owns portable functions/triggers and the SELECT-only AI SQL guard;
  it must preserve the owner/migration/runtime separation and S06 RLS/actor
  boundary.
- **S08** owns the provider-neutral migration ledger, repeatable clean-room,
  dump/restore and reconciliation harness, plus the disposable-Neon gate; it
  must not replace the S06 role contract with an owner apply.
- **S11** owns the Neon driver, connection lifecycle and transaction wrapper:
  validate `tenantId` and S03 ActorContext at the server boundary, then bind
  `SET LOCAL app.actor_id` while using the non-owner runtime role.
- **S15** owns AI/server handlers and system/job identity. Until then,
  `app_system` has no grants and no team-user fixture.
- **S21** owns tenant/machine-scoped ingest, machine credentials and machine
  identity. Until then, `app_machine` has no grants and no login.

## External state and secrets

No deploy, live Neon/Supabase apply, provider resource, credential, data copy or
Git push was performed. The only database executions were disposable local
PostgreSQL clean-room containers, removed by their harnesses. No secrets were
added.
