# N-S06 repair handoff

Status: **complete**

## Session identity

- Repair base: `6be548559e22a54cc3f1cdd2a5a7468da8968bcb`
- Branch: `codex/neon-s04-s06-review-fixes`
- Commit: self-referential; the exact repair SHA is reported in the session
  completion response
- Scope: repair the previously accepted S04/S05 inventory pinning and S06
  identity, role, actor-context and RLS contract

## Implementation

- Corrected the S04 `archive_follow_up_after_last_lead()` final execution ACL
  to `service_role` and made the assertion compare function owner, language,
  security-definer/search-path semantics and final execute ACLs.
- Strengthened S04 semantic checks for trigger timing/events and index
  expressions/predicates, and added the explicit private `agent` artifact
  Storage dependency. The S05 source-inventory SHA and related assertions now
  pin this repaired S04 contract.
- Reworked S06 around a pre-provisioned role contract: `app_owner` owns public
  objects; `app_migration` is a non-superuser login allowed only to `SET ROLE
  app_owner`; `app_runtime` is a separate non-superuser login with no owner or
  migration membership; `app_readonly` is read-only; `app_machine` and
  `app_system` are no-login, no-grant fail-closed principals.
- The S06 artifact no longer bootstraps roles as `postgres`. The clean-room
  applies it through the `app_migration` session after disposable role/schema
  preparation. Runtime assertions prove no object ownership, no `BYPASSRLS`
  and failed `SET ROLE` attempts into owner and migration.
- Kept S03's canonical user/machine/system ActorContext boundary: S06 only
  authorizes user actors by canonical `users.id`; machine/system contexts have
  no S06 grants, with machine identity/ingest owned by S21 and system/job
  identity owned by S15. No machine/system team-user fixtures were added.
- Added active positive DML and missing, malformed, unknown, inactive and
  cross-user negative DML/visibility assertions. Removed the duplicate
  `user_identities_user_id_idx` and `team_members_user_id_idx` indexes.

## Changed files

- `docs/platform-ops/tenant-schema-inventory-v053.json`
- `docs/platform-ops/supabase-dependencies-v053.json`
- `supabase/tests/tenant_inventory_assertions.mjs`
- `docs/platform-ops/portable-business-schema-inventory-v1.json`
- `postgres/tests/portable_business_inventory_assertions.mjs`
- `postgres/tenant-baseline/v1/002_identity_roles_actor_rls.sql`
- `postgres/tenant-baseline/v1/README.md`
- `docs/platform-ops/portable-identity-roles-rls-inventory-v1.json`
- `postgres/tests/portable_identity_roles_rls_inventory_assertions.mjs`
- `postgres/tests/portable_identity_roles_rls_catalog_assertions.sql`
- `postgres/tests/portable_identity_roles_rls_behavior_assertions.sql`
- `postgres/tests/portable_identity_roles_rls_cleanroom.sh`
- `postgres/tests/portable_identity_roles_rls_role_bootstrap.sql`
- `postgres/tests/portable_identity_roles_rls_fixture_seed.sql`
- `postgres/tests/portable_identity_roles_rls_runtime_boundary.sql`
- `docs/implementation-handoffs/N-S06.md`

## Verification

- `node --check supabase/tests/tenant_inventory_assertions.mjs` — passed.
- `node supabase/tests/tenant_inventory_assertions.mjs` — passed: 25 tables,
  8 view declarations / 7 final view objects, 13 functions, 12 triggers, 72
  indexes, 126 constraints, 25 RLS tables, 50 policies and 17 dependencies.
- `node --check postgres/tests/portable_business_inventory_assertions.mjs` —
  passed.
- `node postgres/tests/portable_business_inventory_assertions.mjs` — passed:
  S05 source SHA, corrected S04 ACL pin and agent-artifact dependency checks.
- `node --check postgres/tests/portable_identity_roles_rls_inventory_assertions.mjs`
  and `node postgres/tests/portable_identity_roles_rls_inventory_assertions.mjs`
  — passed; S06 artifact SHA:
  `18a779f3abd99592a1af71430f87b4f93c7beff3fba75924fa0122ae0b3c3d80`.
- `bash -n postgres/tests/portable_identity_roles_rls_cleanroom.sh` — passed.
- `jq empty` over all four changed inventory JSON files — passed.
- `git diff --check` — passed.
- `cd frontend && npm test` — passed, 9 test files / 49 tests.
- `cd frontend && npm run build` — passed (`tsc -b` and Vite production build).
- The S05 and S06 clean-room harnesses were invoked but not applied: local
  Docker daemon is unavailable, and both exited 2 with “clean-room apply was
  not run”. No image pull was attempted.

## External state and secrets

No deploy, live Neon/Supabase apply, external provider change, credential,
data copy or Git push was performed. The clean-room role bootstrap is test-only
and was not executed because Docker was unavailable. No secrets or passwords
were added.

## Known limits and next owner

The S06 artifact is an empty-baseline companion. S11 owns the real driver and
transaction wrapper that binds `tenantId`, validates the server ActorContext
and performs `SET LOCAL app.actor_id`; S15 owns system/job identity and S21
owns machine identity/ingest. S08 must run the disposable PostgreSQL clean-room
apply once a local PostgreSQL image/daemon is available.

S07 may start only from the integrated repair commit and must preserve the
owner/migration/runtime separation, fail-closed machine/system principals,
transaction-local actor contract and S04/S05 inventory pins.
