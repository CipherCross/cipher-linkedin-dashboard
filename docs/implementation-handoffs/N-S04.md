# N-S04 handoff

Status: **complete**

## Session identity

- Base SHA: `2e64436231ae0dba7cfc1c9b6570349863e591cf`
- Branch: `codex/neon-s04-schema-inventory`
- Commit: self-referential; this handoff is included in the single logical
  S04 commit, whose exact SHA is reported in the session completion response
- Scope: machine-readable inventory of the immutable Supabase tenant baseline
  v053 and its Supabase-only dependency/risk list

## Preflight and integration

- Read completely: `AGENTS.md`,
  `specs/2026-07-30-neon-migration-and-multitenancy.md` and
  `docs/implementation-handoffs/N-S03.md`.
- Confirmed the working tree was clean before S04 changes.
- Confirmed `codex/neon-s03-datastore-contracts` pointed to
  `2e64436231ae0dba7cfc1c9b6570349863e591cf`.
- Confirmed `main` was still at S02 `77c0bf336daae7bfe675bd7861d7b83b8738f6c6`.
- Integrated S03 into `main` with `git merge --ff-only`; no reset or rebase was
  used. Created this branch from the resulting S03 SHA.

## Implementation

- Added a machine-readable v053 inventory with manifest/source checksums,
  tables, view declaration history, final view objects, functions, triggers,
  identity sequences, explicit and constraint-backed indexes, all constraint
  categories, RLS tables, policy templates, extensions and provider bootstrap
  requirements.
- Recorded the requested eight views precisely: v053 has eight view definition
  statements, with the final `CREATE OR REPLACE` of `campaign_metrics` leaving
  seven distinct current view objects.
- Recorded 25 tables, 13 functions, 12 triggers, 72 total indexes (38 explicit
  plus 34 constraint-backed), 126 constraints, 25 RLS-enabled tables and 50
  policies.
- Added a machine-readable Supabase-only dependency list covering
  `auth.users`, `auth.uid()`, provider schemas, `anon`/`authenticated`/
  `service_role`, provider ownership/default ACLs, `storage.*`,
  `supabase_migrations`, PostgREST, Auth/Storage SDKs, service-role transport,
  extension parity, and the explicit `supabase_vault`/Realtime absence checks.
- Added a dependency-aware assertion script that verifies the inventory against
  the immutable SQL and manifest without connecting to a database.

## Changed files

- `docs/platform-ops/tenant-schema-inventory-v053.json`
- `docs/platform-ops/supabase-dependencies-v053.json`
- `supabase/tests/tenant_inventory_assertions.mjs`
- `docs/implementation-handoffs/N-S04.md`

No schema, migration, tenant-baseline SQL, runtime SQL, UI, API handler,
DataContext, Auth, Storage, sync-agent or operations runtime file was changed.

## Verification

- `node --check supabase/tests/tenant_inventory_assertions.mjs` — passed.
- `node supabase/tests/tenant_inventory_assertions.mjs` — passed:
  25 tables, 8 view declarations / 7 final view objects, 13 functions,
  12 triggers, 72 indexes, 126 constraints, 25 RLS tables, 50 policies,
  16 dependency records.
- `cd frontend && npm test` — passed, 9 test files / 49 tests.
- `cd frontend && npm run build` — passed (`tsc -b` and Vite production build).
- `git diff --check` — passed.
- `supabase/tests/tenant_baseline_cleanroom.sh` was not run because the required
  local Docker image `public.ecr.aws/supabase/postgres:17.6.1.127` is not
  present; pulling it would be an external network action outside S04. The
  static inventory assertions do not replace the later clean-room apply owned
  by S08.

## External state and secrets

No external mutation occurred: no Neon project, database, R2 bucket, Auth user,
Vercel configuration, deployment, credential, Supabase change, dump or data
copy was created. Nothing was pushed. The only Git ref changes were the
required local S03 fast-forward into `main` and creation of this local S04
branch. The inventory and dependency artifacts contain no secrets or
credentials.

## Known limits

- The inventory is source-derived and checksum-pinned; it does not introspect a
  live Supabase or Neon database. S08 owns clean-room apply and live catalog
  reconciliation.
- Source extension measurements include `plpgsql`, `pg_stat_statements`,
  `uuid-ossp` and `supabase_vault`; only `pgcrypto` is declared by v053. Their
  live availability and ownership remain a later clean-room/provider check.
- The dependency list records migration risks and ownership boundaries; it does
  not implement identity, RLS, object storage, API, ingest or provider adapters.

## Exact start point for S05

Start S05 from this single S04 commit after it is fast-forward integrated into
`main`. S05 owns the portable business tables, indexes, views and constraints
baseline, using this inventory as its source contract. S05 must not implement
identity schema, actor/RLS SQL, portable functions/triggers, Neon driver or
runtime adapters; those remain with S06-S08 and later sessions.

**S05 не начиналась.**
