# N-S01 handoff

Status: **complete**

## Session identity

- Base SHA: `93eb9d8d640548fc40151443f60044c5ef58b786`
- Branch: `codex/neon-s01-measurements`
- Commit: `docs(neon): record source migration measurements` (the exact SHA of
  the commit containing this handoff is reported in the final S01 response;
  embedding a commit's own SHA in that same commit is self-referential)
- Scope: S01 read-only source measurements only

## Changed files

- `docs/platform-ops/neon-migration-source-measurements-queries.sql`
- `docs/platform-ops/neon-migration-source-measurements.md`
- `docs/implementation-handoffs/N-S01.md`

No runtime, production migration, schema, Auth, Storage, frontend, sync-agent or
operations-core file changed.

## Aggregate measurement result

- PostgreSQL: `17.6`
- Database size: `29,224,083 bytes` (`28 MB`)
- Public table+index relations: `15,851,520 bytes`
- Storage: `602` objects and `18,359,103 bytes` (`18 MB`)
- Largest public relations:
  - `messages`: 5,840 rows, `5,242,880 bytes`;
  - `leads`: 3,455 rows, `5,185,536 bytes`;
  - `events`: 4,520 rows, `1,966,080 bytes`;
  - `sync_runs`: 8,702 rows, `1,171,456 bytes`.
- Migration ledger: 54 contiguous versions, `001` through current `054`
- Installed extensions: `pgcrypto 1.3`, `plpgsql 1.0`,
  `pg_stat_statements 1.11`, `uuid-ossp 1.1`,
  `supabase_vault 0.3.1`
- Approximate 527-second cumulative-stat delta:
  `+2,311` committed transactions, `+155` inserted tuples,
  `+15,496` updated tuples and `+150` deleted tuples.
- Snapshot B conflicts/deadlocks: `0` / `0`

Storage bucket counts and byte totals reconcile exactly. The write counters
share the same `stats_reset`; the first capture time is an approximate
screenshot-derived timestamp. Byte growth remains unknown because only the
second snapshot includes complete sizes/counts.

## Preliminary downtime input

The measured size supports `pg_dump`/`pg_restore` as the default. Initial
planning input is 5–15 minutes for database dump/transfer/restore, 30–60 minutes
for the first database rehearsal with reconciliation, and a provisional 60–90
minute full maintenance window. These ranges are not an SLA and must be replaced
with rehearsal timings.

Logical replication remains a fallback only if rehearsal exceeds the
owner-approved downtime window.

## Verification performed

- Confirmed the owner-provided result is valid JSON.
- Repeated all database, relation, row-count and time-window values from the
  source result.
- Recomputed Storage bucket count and byte totals; both match exactly.
- Recomputed the two `pg_stat_database` snapshot deltas and approximate rates.
- Confirmed both snapshots have the same `stats_reset`.
- Confirmed migration versions are contiguous from `001` through `054`.
- Confirmed the canonical SQL contains one read-only `WITH … SELECT` statement
  and no DDL/DML/RPC.
- Reviewed the query output and documentation for prohibited secrets and
  sensitive row/object content; none is present.
- Ran diff formatting and scope checks and reviewed the full diff before commit.

## Remaining unknowns

- Measured compressed dump size, dump time and restore time on the selected
  Neon tier/region.
- Longer-window physical size and exact-row growth.
- Object checksums and copy throughput.
- Neon extension/ownership/grant compatibility.
- Auth/Storage provider decisions, RPO/RTO and approved downtime window.
- Final freeze, reconciliation and sync-agent catch-up duration.

No credentials or secrets were requested, discovered or stored.

## Exact start point for S02

Start S02 only after this S01 commit is accepted/merged. Use the archived
Supabase P4-C checkpoint `c8a9d5f35694dd6e8f7f35643967c8ed9808ced5`
and the measurement artifact to create the ADR and provider decision matrix.
S02 remains documentation/checkpoint work: it must not create a Neon project or
mutate Supabase, Vercel, Auth, Storage or tenant resources.

The S02 G0 owner inputs are:

- Neon region and tier;
- downtime window, RPO and RTO;
- Auth candidate;
- S3-compatible Storage candidate;
- confirmation that Vercel remains the temporary hosting adapter.

**S02 не начиналась.**
