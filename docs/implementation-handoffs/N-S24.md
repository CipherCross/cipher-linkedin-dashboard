# Neon migration — S24 implementation handoff

Date: 2026-08-07  
Branch: `main`  
Starting commit: `3685b9415c7418db374d4d05c1dac42f3b75c4bb` (`3685b94`)  
Implementation commit: `cb5af1ec601ec16c1e105e74681deb9e83459adf`  
Remote state: `origin/main` remains `05bb030`; nothing was pushed.  
The historical `codex/neon-agent-dual-transport` ref at `d1677e3` was not used.

## Scope completed

S24 only: provider-neutral operations catalogs, plans, closed JSON Schemas,
registry migration, and fake/contract coverage. S25 was not started.

- Added the strict catalog snapshot contract for regions, provider tiers,
  pricing, backups, release compatibility, capabilities, and subprocessors.
  Catalog snapshots reject unknown fields, duplicate IDs, malformed digests,
  invalid review status, and invalid entry availability at the runtime
  resolver boundary; the JSON Schema is closed and types every allowlisted
  field.
- Changed the canonical plan vocabulary to capability names (`data`,
  `identity`, `object_storage`, `hosting`, `domain`, `email`, and
  `source_repository`). Onboarding effects, resource names, business inputs,
  environment policy, provider snapshots, MCP summaries, and preflight/planner
  execution context use the neutral vocabulary.
- Added registry schema v1→v2 migration. Tenant tier columns and operation
  steps use neutral names; resource references retain opaque owner/resource IDs
  while mapping only known legacy kinds. Unknown legacy resource kinds fail the
  migration closed. Registry version, plan digest, and idempotency behavior are
  preserved.
- Kept the existing provider-specific adapter/checkpoint implementation behind
  the neutral capability fields so this session does not begin S25 adapter
  work. Fakes and contract fixtures use the neutral fields.
- Added registry migration coverage, unknown catalog-entry coverage, strict
  catalog contract coverage, and updated fake/hosting/P4-B/P4-C/MCP/security
  assertions and stable digests.

## Invariants preserved

- Workflow remains `preflight → read-only plan → owner approval → apply/resume
  → verify`; planning and preflight do not write provider state.
- Apply still requires the exact unexpired plan digest, expected registry
  version, blocker-free prerequisites, current provider snapshots, and the
  caller-stable idempotency key. Resume uses the original operation and plan.
- Unknown, expired, deprecated, unavailable, unapproved, or unpriced catalog
  references block planning/apply. Provider response fragments are not accepted
  as catalog fields.
- Secrets remain Keychain references/labels only. No provider payloads, raw
  responses, shell/SQL/HTTP commands, or secret values enter plans, registry,
  audit, fixtures, logs, or tool output.
- Provider resource IDs remain opaque registry/adaptor data. Failed effects do
  not trigger automatic deletion, down migration, or admin invitation.
- Existing immutable PostgreSQL migration/baseline artifacts were not changed.

## Verification

- `cd ops && npm test` — 72 passed, 0 failed. This includes the fake onboarding
  flow, stable plan/MCP digests, registry version/idempotency checks, catalog
  validation, and v1→v2 registry migration test.
- `node postgres/tests/portable_migration_ledger_static_assertions.mjs` — 188
  static assertions passed, including immutable baseline and provider/credential
  sweeps.
- `git diff --check` — passed.
- Frontend typecheck/build was not run because no shared frontend/API code was
  changed.
- No `supabase db push`, Vercel deploy, `deploy.sh`, push, live provider call,
  live catalog refresh, tenant provisioning, or external verification was run.

## Known limits and owner gates

- The concrete P4-C adapter/runtime still has provider-specific implementation
  types and fixture IDs behind the new neutral fields. Neon, Better Auth,
  Cloudflare R2, and the next hosting adapter implementation remain S25 work.
- The current planner/executor compatibility path remains limited to the
  existing disposable onboarding scope. This commit does not authorize general
  tenant provisioning or any live apply.
- Catalog freshness, provider credentials, owner scopes, and live capability
  limits were not verified. A later live operation requires a new preflight and
  explicit owner approval for the exact plan digest, registry version, and
  idempotency key.
- Registry implementation schema is now v2; onboarding/release plan schema
  versions remain v1 by design and were not silently changed.

## Exact next-session starting point

Start from the clean `main` HEAD produced by this session (the implementation
commit above plus this handoff-only commit), verify `git status` and the exact
HEAD before editing, and do not use `d1677e3`. Read this handoff, the S25 row in
`specs/2026-07-30-neon-migration-and-multitenancy.md`, and
`docs/platform-ops/operations-contract-v1.md`. Continue with S25 only after its
scope is approved; keep live provider execution and tenant provisioning gated.
