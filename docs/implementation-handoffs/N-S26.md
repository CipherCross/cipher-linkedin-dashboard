# Neon migration — S26 implementation handoff

Date: 2026-08-07
Session: S26
Base HEAD: `5b48f7a46b8d3f5aa6a25a0d7f4590a2c4ce3c45`
Branch: `main`
Remote state: nothing was pushed

## Scope and gate outcome

S26 was limited to read-only preflight and plan validation because the exact G4
owner approval is absent. No owner decision was found for the exact plan digest,
expected registry version, caller-stable idempotency key, provider scope, or
restore scope. The apply gate was not crossed.

S27 and S28 were not started. No external-tenant onboarding, production
cutover, Supabase decommissioning, break-glass deletion, external provider/API call,
secret operation, deployment, invite, or restore was performed.

## Read-only validation

The S25 canonical adapter/runtime wiring and tests were inspected before
validation. The fake bundle exposes only the approved capability names:
`data`, `identity`, `objectStorage`, `hosting`, `email`, `domain`, and
`sourceRepository`. The onboarding executor preserves the ordered 13-step
sequence, persists resource references before step success, retains a provider
request ID for `outcome_unknown`, quarantines failed work, and resumes by
deterministic ownership/idempotency behavior in the existing fake coverage.

The fresh in-memory fake-provider dry run used the provider-neutral disposable
fixture and completed read-only preflight with zero blockers:

- provider snapshots: 5, each valid through `2030-01-01T00:30:00.000Z`;
- plan state: `valid` in the dry-run result;
- effects: 13 ordered effects, including baseline 053, migration 054, private
  storage/identity/email, support membership, hosting/env/domain/build/deploy,
  smoke suite, admin invite, and finalization;
- recovery coverage declared: database schema/data, Auth configuration and
  identities, Storage metadata, private objects or approved reconstruction, and
  deployment/configuration metadata.

This was a disposable in-memory validation only. It did not persist the plan or
create an operation.

## Exact plan identity

These values are the deterministic read-only dry-run result, not an approved or
consumed apply request:

- `plan_id`: `pln_c3ce1364c1b51feadf3fde25`
- `plan_digest`: `sha256:cde42283b18cd999762aeedb992d480acb6d81336d2996cdcd772c2afc0bdd74`
- `expected_registry_version`: `1` in a fresh in-memory registry
- `idempotency_key`: none used; no apply/resume request was submitted

The digest is therefore a validation artifact, not evidence of owner approval,
resource creation, schema application, deployment, or restore. A future apply
must use the exact unexpired persisted plan and a newly recorded caller-stable
idempotency key approved for that plan, registry version, provider scope, and
restore scope.

## Verification evidence

- `cd ops && npm test` — **78 passed, 0 failed**.
- `node postgres/tests/portable_migration_ledger_static_assertions.mjs` —
  **188 passed, 0 failed**.
- The existing local suite includes fake coverage for ordered onboarding,
  smoke-before-invite, failure quarantine, outcome-unknown adoption, duplicate-
  free resume, secret-free provider results, deterministic ownership, and
  encrypted registry-backup recovery. Those tests are contract evidence only;
  they are not a live Neon onboarding or tenant restore result.
- Provider clean-room checks were not run because they were not explicitly
  authorized.
- No frontend/API code changed; the frontend build/typecheck was not run.

## Apply/resume/verify and restore evidence

There is no S26 apply/resume/verify evidence: the exact G4 approval was missing,
so no operation was started and no external provider effect was attempted. In particular,
there are no Neon project, identity, private bucket, hosting project, env,
domain, build, schedule, smoke, admin-invite, or external resource IDs to
report.

There is no S26 tenant backup/restore-drill evidence. The declared restore scope
is preserved in the plan shape, but schema/data, identity, storage, RLS, and
deployment metadata were not restored or externally verified. The passing
registry-backup test covers only the local operations registry and must not be
described as tenant-data recovery.

## Preserved invariants

- Required flow remains `preflight → plan → owner approval → apply/resume →
  verify`.
- Apply remains gated by the exact plan digest, expected registry version,
  provider snapshot match, and caller-stable idempotency key.
- No raw provider, SQL, shell, HTTP, DNS, environment, or secret lifecycle
  command was used.
- No secret values, provider responses, credentials, or raw environment values
  were written to registry, audit, plans, results, errors, or this handoff.
- Deterministic names and matching ownership markers remain required for
  adoption; ambiguous or mismatched ownership remains quarantined.
- Failure does not auto-delete resources or invite an admin; an outcome-unknown
  step retains its opaque provider request ID for reviewed resume.
- Immutable baseline/migration artifacts were not changed. No `supabase db push`,
  Vercel deploy, `deploy.sh`, Git push, or external provider mutation was run.

## Known limits and owner gates

G4 must be recorded before the next apply-capable session and must cover, as one
exact intent:

1. the persisted unexpired plan digest and plan ID;
2. the expected registry version;
3. the caller-stable idempotency key to reuse for every retry/resume;
4. the approved disposable provider scope and resource effects; and
5. the complete backup/restore scope, including schema/data, identity, private
   storage/RLS, and deployment metadata verification.

After approval, apply/resume may use only the operations core and approved
adapters. Any provider failure or outcome-unknown result must remain quarantined
until reviewed; retry must use the same operation and idempotency key and must
not create duplicates. External verification and any live clean-room/provider
check remain separately owner-gated.

## Exact next-session starting point

Start from clean `main` at the commit containing this handoff. Re-verify branch,
exact HEAD, and clean worktree; read this handoff, the S26 row/G4 sequencing
rules, and the operations contract. Do not start S27 or S28. Continue only when
the exact G4 approval is present; otherwise repeat read-only preflight/plan
validation and stop at the apply gate.
